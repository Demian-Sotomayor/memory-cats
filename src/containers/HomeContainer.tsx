import { Board } from '@/components'
import React from 'react'

export const HomeContainer = () => {
  return (
    <div>
      {/* Contenedor */}
      <section>
        <Board />
      </section>
    </div>
  )
}
